version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mynodeapp
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - sqlite_data:/app/data

volumes:
  sqlite_data:
